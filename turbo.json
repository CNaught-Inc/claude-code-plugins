{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "outputs": ["dist/**"] },
    "test": { "dependsOn": ["build"] },
    "typecheck": {},
    "lint": {},
    "clean": { "cache": false }
  }
}
