{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "outputs": ["dist/**"], "env": ["NO_SOURCEMAP", "MINIFY"] },
    "test": { "dependsOn": ["build"] },
    "typecheck": {},
    "lint": {},
    "clean": { "cache": false }
  }
}
